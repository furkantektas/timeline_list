name: example
description: A showcase application demonstrating the timeline_list package's features, including vertical timelines with custom markers, icons, and various positioning options.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  timeline_list:
    path: ../

flutter:
  uses-material-design: true
  assets:
    - assets/images/
